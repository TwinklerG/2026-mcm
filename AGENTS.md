# 美赛代码

## 核心原则

- 代码应一次性写出高质量版本，无需注释标记修改历史
- 不使用"新增"、"修复"、"v1.2"等版本标记
- 使用 Git 管理版本，代码本身应保持整洁
- 框架接口如有变化，同步更新文档、示例代码与接口文档。
- 模型只需要输出数据和图表等核心内容，禁止出现解释、建议等内容
- 图表、数据等输出需保持一致性和可重复性

## 代码规范

- 优先使用 Polars 进行数据处理
- 必须类型提示
- NumPy 风格文档字符串
- ruff 格式化与检查
- 简洁明了的注释与文档

## 输出要求

- 图表：`figures/` 目录，PNG/PDF/SVG
- 表格：Typst 导出
- 模型：`models/` 目录
- 结果：`outputs/` 目录（JSON）

## 验证

```bash
uv run ruff check      # 代码检查
uv run ruff format     # 代码格式化
```